name: (Disabled) Build SpotlessFilm Executables

on:
  workflow_dispatch:  # Manual only; jobs are disabled below with if: false

jobs:
  build:
    if: ${{ false }}  # Entire job disabled
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
        
    runs-on: ${{ matrix.os }}
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller
        pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
        pip install pillow customtkinter opencv-python numpy
        
    - name: Build executable (Windows)
      if: matrix.os == 'windows-latest'
      run: |
        cd src
        pyinstaller --clean --onefile --windowed --name SpotlessFilm spotless_film_modern.py
        
    - name: Build executable (macOS)
      if: matrix.os == 'macos-latest'
      run: |
        cd src
        pyinstaller --clean --onefile --windowed --name SpotlessFilm spotless_film_modern.py
        
    - name: Build executable (Linux)
      if: matrix.os == 'ubuntu-latest'
      run: |
        cd src
        pyinstaller --clean --onefile --windowed --name SpotlessFilm spotless_film_modern.py
        
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: SpotlessFilm-${{ matrix.os }}
        path: |
          src/dist/SpotlessFilm*
          
  release:
    if: ${{ false }}  # Entire job disabled
    needs: build
    runs-on: ubuntu-latest
    # if: github.ref == 'refs/heads/main'  # superseded by the disabled guard above
    
    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ github.run_number }}
        name: SpotlessFilm Release v${{ github.run_number }}
        files: |
          SpotlessFilm-windows-latest/*
          SpotlessFilm-macos-latest/*  
          SpotlessFilm-ubuntu-latest/*